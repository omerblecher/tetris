---
phase: 02-react-shell-visual-polish
plan: 07
type: execute
wave: 5
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
  - "02-05"
  - "02-06"
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05
  - VIS-06
  - CTR-01
  - CTR-02
  - CTR-03
  - CTR-04
  - CTR-05
  - LAY-01
  - LAY-02
  - LAY-03
  - LAY-04

must_haves:
  truths:
    - "All 15 Phase 2 requirements are satisfied and visually verifiable"
    - "Game is fully playable with keyboard on desktop"
    - "Game is fully playable with touch on mobile (no keyboard required)"
    - "Layout shows three-column desktop and single-column mobile without overflow"
    - "All three flash animations fire correctly during gameplay"
  artifacts:
    - path: "src/hooks/useKeyboard.ts"
      provides: "DAS/ARR keyboard hook"
      exports: ["useKeyboard"]
    - path: "src/hooks/useTouchControls.ts"
      provides: "Swipe/tap touch hook"
      exports: ["useTouchControls"]
    - path: "src/components/SidePanel.tsx"
      provides: "Score/Hold side panels"
      exports: ["SidePanel"]
    - path: "src/components/VirtualControls.tsx"
      provides: "Mobile virtual buttons"
      exports: ["VirtualControls"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/GameBoard.tsx"
      via: "All display state + callbacks passed as props from App"
      pattern: "GameBoard"
    - from: "src/hooks/useGameEngine.ts"
      to: "src/renderer/CanvasRenderer.ts"
      via: "rendererRef triggers animations on engine events"
      pattern: "triggerLineClear|triggerLockFlash|triggerLevelUp"
---

<objective>
Human verification checkpoint for Phase 2. Verify all 15 requirements (VIS, CTR, LAY) are satisfied across desktop and mobile environments before marking Phase 2 complete.

Purpose: Phase 2 is complete when a human verifies the game is fully playable on both keyboard and touch, all visual effects work, and the layout is responsive.
Output: Human approval or list of issues to fix.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build check + dev server launch</name>
  <files></files>
  <action>
Run the following checks and report results:

1. `npm run build` — must complete with 0 TypeScript errors and 0 warnings
2. `npm test` — all tests must pass (Board, Bag, Scorer — 3 test files)
3. `npm run dev` — start dev server and report the local URL

If build or tests fail, fix the issues before proceeding to the human verification checkpoint.

Report which plans were executed and any deviations noted in SUMMARY.md files.
  </action>
  <verify>npm run build exits 0. npm test exits 0 with all tests passing. Dev server starts on localhost:5173 (or similar).</verify>
  <done>Build clean. Tests pass. Dev server running. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 2 complete implementation:
- Plan 02-01: Synthwave neon palette, ghost piece as outline-only, clearLines returns row indices
- Plan 02-02: Animation state machine — line-clear flash (100ms), lock-in flash (50ms), level-up flash (300ms)
- Plan 02-03: DAS/ARR keyboard hook (133ms delay, 33ms repeat), P-key pause/resume
- Plan 02-04: CSS Grid three-column layout, Orbitron font, SidePanel (Hold + Score/Level/Lines/Next/Best), DPR canvas scaling, personal best in localStorage
- Plan 02-05: Touch swipe/tap gestures on canvas, VirtualControls (CCW/DROP/HOLD/PAUSE buttons) for mobile
- Plan 02-06: Game-over React overlay (score + best + PLAY AGAIN + disabled LEADERBOARD), pause React overlay (PAUSED + RESUME)
  </what-built>
  <how-to-verify>
Run `npm run dev` and open http://localhost:5173 in a browser.

**DESKTOP CHECKS (keyboard controls):**

1. **VIS-01/02** — Board renders with vivid neon colors per piece type (cyan I, blue J, orange L, yellow O, green S, purple T, pink Z). Background is near-black. Grid lines are faint dark blue.

2. **VIS-05** — Ghost piece is a colored outline rectangle with no solid fill.

3. **VIS-06** — Lock a piece (wait for auto-lock or press spacebar for hard drop) — a brief white flash should appear on the locked cells (~50ms).

4. **VIS-03** — Clear a line by filling a row — the row flashes bright white before collapsing (~100ms).

5. **VIS-04** — Reach level 2 (clear 10 lines) — board should pulse with a brief pink/purple overlay (~300ms).

6. **LAY-01** — Desktop: three-column layout (Hold panel on left, board in center, Score/Level/Lines/Next/Best on right).

7. **CTR-01** — Keyboard controls: arrows move/rotate, space = hard drop, Z = CCW rotate, C = hold, soft drop with down arrow.

8. **CTR-01 DAS/ARR** — Hold left arrow: piece moves once, then after ~133ms starts auto-repeating smoothly.

9. **CTR-04** — Press P: game pauses with PAUSED overlay. Press P again: game resumes.

10. **CTR-05** — Let the game end: GAME OVER overlay appears on board (board visible underneath), shows SCORE and BEST values, PLAY AGAIN button restarts. LEADERBOARD button visible but disabled.

**MOBILE CHECKS (open DevTools → mobile simulation → iPhone SE 375px or real device):**

11. **LAY-02** — Mobile: board at top, score panel below, virtual controls at bottom. No horizontal overflow.

12. **LAY-03** — Canvas fits viewport height without vertical scroll in portrait mode.

13. **LAY-04 / CTR-02** — Swipe left/right on canvas: piece moves. Swipe down: soft drop. Tap: rotates CW. Swipe up: hard drop.

14. **CTR-03** — Four virtual buttons visible below board: ↺ CCW, ⬇ DROP, ⬚ HOLD, ⏸ PAUSE — all tap to function.

15. **Desktop virtual buttons hidden** — Switch back to desktop view: virtual buttons should NOT be visible.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g., "VIS-03 flash not visible", "mobile layout overflows on right side")</resume-signal>
</task>

</tasks>

<verification>
All 15 Phase 2 requirements satisfied as verified by human:
- VIS-01: Neon colors per piece type on canvas
- VIS-02: Near-black background, faint grid lines
- VIS-03: Line-clear row flash
- VIS-04: Level-up board flash
- VIS-05: Ghost piece outline-only
- VIS-06: Lock-in mino flash
- CTR-01: Full keyboard controls with DAS/ARR
- CTR-02: Swipe/tap touch controls on mobile
- CTR-03: Virtual on-screen buttons visible on mobile
- CTR-04: Pause/resume with P key and pause button
- CTR-05: Game-over screen with score, best, restart, leaderboard placeholder
- LAY-01: Desktop three-column layout
- LAY-02: Mobile single-column stacked layout
- LAY-03: Canvas scales to viewport without overflow
- LAY-04: Fully playable via touch on mobile
</verification>

<success_criteria>
Human approves all 15 checks. Phase 2 is complete and ready for Phase 3 (Firebase Auth + Leaderboard).
</success_criteria>

<output>
After human approval, create `.planning/phases/02-react-shell-visual-polish/02-07-SUMMARY.md` documenting:
- All 15 requirements as verified
- Any issues found and fixed during verification
- Phase 2 completion date and notes for Phase 3 handoff
</output>
