---
phase: 03-firebase-auth-leaderboard
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/components/GameBoard.tsx
autonomous: true

requirements:
  - AUTH-04
  - LDB-02

must_haves:
  truths:
    - "Game-over overlay shows 'New personal best!' banner (neon celebratory style) only when isNewPersonalBest is true"
    - "Authenticated users on game-over see their Firestore personal best in the BEST display"
    - "Guest players see a prominent 'Sign in with Google to save your score' button on the game-over overlay"
    - "Guest sign-in button on game-over overlay calls signIn() from useAuth — not a link, a button"
    - "Previously disabled LEADERBOARD button is now enabled (not disabled/not-allowed)"
    - "Authenticated users do NOT see the guest sign-in prompt on game-over overlay"
  artifacts:
    - path: "src/components/GameBoard.tsx"
      provides: "Extended game-over overlay with new PB banner, guest sign-in button, active leaderboard button"
      contains: "isNewPersonalBest"
  key_links:
    - from: "src/components/GameBoard.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuth() for user state and signIn function"
      pattern: "useAuth"
    - from: "src/components/GameBoard.tsx"
      to: "displayState.isNewPersonalBest"
      via: "prop or destructured from useGameEngine return"
      pattern: "isNewPersonalBest"
---

<objective>
Update the GameBoard game-over overlay to surface Phase 3 features: "New personal best!" celebration banner, guest sign-in button, and an activated LEADERBOARD button.

Purpose: This plan completes the player-facing Firebase integration — the game-over overlay is where authenticated players learn they set a new PB, and where guests are prompted to sign in.
Output: Updated src/components/GameBoard.tsx with extended game-over overlay.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-firebase-auth-leaderboard/03-CONTEXT.md
@src/components/GameBoard.tsx
@src/hooks/useGameEngine.ts
@.planning/phases/03-firebase-auth-leaderboard/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Game-Over Overlay in GameBoard.tsx</name>
  <files>
    src/components/GameBoard.tsx
  </files>
  <action>
    Read `src/components/GameBoard.tsx` fully before making changes. The file was restructured in Phase 2 Plan 04 and 06.

    Current game-over overlay structure (from Phase 2):
    - "GAME OVER" title in `#ff2060`
    - Final SCORE in cyan
    - BEST in neon yellow
    - PLAY AGAIN button
    - LEADERBOARD button: currently `disabled`, `opacity: 0.5`, `cursor: not-allowed`

    GameBoardProps currently receive: `canvasRef`, `engineRef`, `isGameOver`, `onRestart`, `onTogglePause`, `isPaused`, `score`, `bestScore`.

    **Changes required:**

    **1. Add `isNewPersonalBest: boolean` to `GameBoardProps`.**
    This is now part of `displayState` returned by `useGameEngine` (added in Plan 03-02). Pass it from App.tsx to GameBoard just like `score` and `bestScore`.

    **2. Import `useAuth` from `../contexts/AuthContext`** inside GameBoard.tsx. Call `useAuth()` to get `{ user, signIn }` for conditional rendering in the overlay.

    **3. Add "New personal best!" banner** inside the game-over overlay, shown only when `isNewPersonalBest === true`. Style it as celebratory — neon flash / banner feel:
    ```typescript
    {isNewPersonalBest && (
      <div style={{
        fontSize: '13px',
        letterSpacing: '2px',
        color: 'var(--color-accent)',
        textShadow: '0 0 12px var(--color-accent), 0 0 24px var(--color-accent)',
        fontFamily: 'Orbitron, monospace',
        marginBottom: '10px',
        animation: 'none',  // no CSS animation needed; glow alone is celebratory
      }}>
        NEW PERSONAL BEST!
      </div>
    )}
    ```
    Place this banner between the "GAME OVER" title and the score display so it draws the eye immediately.

    **4. Add guest sign-in button** inside the game-over overlay, shown only when `user === null` (guest). Per user decision (locked): this must be a button, not a text link; it should be prominent. Style it matching the existing overlay button style but using neon green or accent color to stand out:
    ```typescript
    {!user && (
      <button
        onClick={() => signIn()}
        style={btnStyle('var(--color-accent)')}
      >
        SIGN IN WITH GOOGLE TO SAVE YOUR SCORE
      </button>
    )}
    ```
    Place this button after the score/best display and before PLAY AGAIN, so the call-to-action is visible before the restart option.

    **5. Enable the LEADERBOARD button** — remove `disabled` attribute, `opacity: 0.5`, and `cursor: not-allowed`. The leaderboard is now always visible in the right panel, so this button can scroll to it or simply serve as a visual anchor. For now, make it a functional button (not disabled) that does nothing on click (no handler needed — the leaderboard is already visible in the side panel). Future phases can wire navigation if needed.

    **6. Pass `isNewPersonalBest` from App.tsx**: Also update `src/App.tsx` to pass `isNewPersonalBest={displayState.isNewPersonalBest}` to `<GameBoard>`. Read App.tsx first to find the GameBoard usage.

    The final game-over overlay content order:
    1. "GAME OVER" title (existing, keep)
    2. "NEW PERSONAL BEST!" banner (new, conditional on isNewPersonalBest)
    3. SCORE display (existing, keep)
    4. BEST display (existing, keep)
    5. "SIGN IN WITH GOOGLE TO SAVE YOUR SCORE" button (new, conditional on !user)
    6. PLAY AGAIN button (existing, keep)
    7. LEADERBOARD button (existing, now enabled)

    Important: `useAuth()` requires `<AuthProvider>` to be an ancestor. GameBoard is rendered inside App.tsx which is inside AuthProvider (wired in Plan 03-03). This is correct.
  </action>
  <verify>
    `npx tsc --noEmit` passes with no errors.
    `grep "isNewPersonalBest" src/components/GameBoard.tsx` shows it in props interface and in conditional render.
    `grep "useAuth" src/components/GameBoard.tsx` shows import and call.
    `grep "disabled" src/components/GameBoard.tsx` returns no match for the LEADERBOARD button (previously disabled, now enabled).
    `grep "isNewPersonalBest" src/App.tsx` shows it passed as prop.
  </verify>
  <done>
    Game-over overlay shows "NEW PERSONAL BEST!" banner when isNewPersonalBest is true. Guest users see "SIGN IN WITH GOOGLE TO SAVE YOUR SCORE" button that calls signIn(). LEADERBOARD button is enabled. Authenticated users do not see the guest sign-in prompt. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- `grep "isNewPersonalBest" src/components/GameBoard.tsx` returns matches in props and JSX
- `grep "SIGN IN WITH GOOGLE" src/components/GameBoard.tsx` confirms guest button text
- `grep "disabled" src/components/GameBoard.tsx | grep -i "leaderboard"` returns nothing (button no longer disabled)
</verification>

<success_criteria>
- Game-over overlay has isNewPersonalBest prop wired from App.tsx displayState
- "NEW PERSONAL BEST!" banner renders with neon glow when isNewPersonalBest is true, hidden otherwise
- Guest sign-in button is a prominent button (not a link) calling signIn() from useAuth
- Authenticated users see no guest sign-in prompt on game-over
- LEADERBOARD button is enabled (not disabled, cursor pointer)
- No TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-firebase-auth-leaderboard/03-04-SUMMARY.md`
</output>
