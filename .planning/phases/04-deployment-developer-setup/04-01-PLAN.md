---
phase: 04-deployment-developer-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - DEV-01
  - DEV-04

must_haves:
  truths:
    - "CLAUDE.md exists in repo root and documents the locked architecture decisions (Canvas, useRef, rAF, SRS, Firebase modular SDK)"
    - "CLAUDE.md contains an explicit never-do list with reasons"
    - "CLAUDE.md contains GSD workflow notes referencing .planning/ directory"
    - "README.md exists in repo root with live URL placeholder, features list, tech stack table, local setup instructions, env var list, and architecture notes"
    - "Firebase config code reads from import.meta.env.VITE_* (already implemented — verified in src/firebase/config.ts)"
    - ".env is in .gitignore and .env.example is committed with all 6 VITE_FIREBASE_* keys"
  artifacts:
    - path: "CLAUDE.md"
      provides: "AI coding assistant project guide"
      min_lines: 80
      contains: "never"
    - path: "README.md"
      provides: "Portfolio-quality project documentation"
      min_lines: 80
      contains: "VITE_FIREBASE"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/"
      via: "GSD workflow section"
      pattern: "\\.planning"
    - from: "README.md"
      to: "VITE_FIREBASE_*"
      via: "Environment Variables section"
      pattern: "VITE_FIREBASE"
---

<objective>
Create CLAUDE.md (AI assistant project guide) and README.md (portfolio documentation), and verify the Firebase environment variable setup is correctly in place.

Purpose: CLAUDE.md locks in the architectural decisions for every future AI session; README.md serves as portfolio documentation and contributor onboarding. DEV-04 (env vars) is already structurally complete — this plan confirms and documents it.

Output: CLAUDE.md, README.md, verified env var state
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-developer-setup/04-CONTEXT.md
@.planning/phases/04-deployment-developer-setup/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md with architecture decisions, never-do list, and GSD workflow notes</name>
  <files>CLAUDE.md</files>
  <action>
Create CLAUDE.md in the repo root. Target: 100-180 lines. Every line competes for context window space — be dense and authoritative, not verbose.

Required sections (per user decision — locked):

**1. Project Overview** (3-5 lines)
Browser-based Tetris with neon/synthwave aesthetic and global Firebase leaderboard.
Stack: React 19 + Vite 6 + TypeScript 5.7 + Canvas 2D + Firebase Auth + Firestore.
Include the live URL placeholder comment exactly as specified: `<!-- Live URL: https://YOUR-PROJECT.vercel.app -->`

**2. Commands** (exact commands, no elaboration)
- `npm run dev` — local dev server (localhost:5173)
- `npm run build` — TypeScript check + Vite production build (`tsc -b && vite build`)
- `npm test` — Vitest unit tests
- `npm run lint` — ESLint

**3. Stack (exact versions)**
- React 19, Vite 6, TypeScript 5.7
- Firebase 12 (modular SDK — tree-shakeable imports only: `firebase/app`, `firebase/auth`, `firebase/firestore`)
- HTML5 Canvas 2D (no game framework — Phaser/Pixi/Three are overkill)
- CSS custom properties (no CSS-in-JS)
- Hosting: Vercel

**4. Architecture Decisions — WHY (per user decision — locked)**
Each decision must state WHY, not just what:
- **Canvas not DOM**: CSS glow on 200 DOM cells causes paint storms on mobile (40fps on mid-range Android). Canvas with pre-baked OffscreenCanvas textures maintains 60fps.
- **useRef not useState for game state**: React setState in the game loop causes ~60 re-renders/sec — performance collapses at higher levels. `useRef` gives mutable state without triggering re-renders. React state is used ONLY for score/level/lines/gameOver display.
- **requestAnimationFrame + delta-time not setInterval**: setInterval drifts by tens of ms per tick, causing erratic piece fall at high levels. rAF + delta-time is the browser's native animation primitive.
- **SRS rotation with wall kick tables**: Naive rotation without wall kicks makes the game feel broken to experienced players — pieces get stuck near walls and floors.
- **Firebase modular SDK (tree-shakeable imports)**: Reduces bundle size; named imports from `firebase/auth` etc. are tree-shaken by Vite.
- **Firestore one-doc-per-user**: One document per UID in `scores/` collection (not one per game). `setDoc` with merge — safe for first-time users.

**5. Never-Do List (per user decision — locked, must include these exact prohibitions)**
- Never use `useState` for game loop state (causes 60 re-renders/sec — use `useRef`)
- Never use `setInterval` for the game loop (use `requestAnimationFrame` + delta-time)
- Never apply CSS glow to DOM cells (Canvas only — prevents paint storms on mobile)
- Never add new Firestore fields without updating security rules in `firestore.rules`
- Never import Firebase using the compat layer (`firebase/compat/*`) — always use modular imports
- Never commit `.env` — real API keys must never appear in git history

**6. File Map** (brief, 1 line each)
```
src/engine/       — pure TS game engine (no React deps): Board, Piece, Scorer, TetrisEngine
src/renderer/     — Canvas 2D rendering: CanvasRenderer, offscreen texture cache
src/firebase/     — Firebase init, auth functions, Firestore leaderboard functions
src/hooks/        — useGameEngine (rAF loop + engine lifecycle), useKeyboard, useLeaderboard
src/components/   — React UI: GameBoard, SidePanel, Leaderboard, AuthHeader, VirtualControls
src/contexts/     — AuthContext (AuthProvider + useAuth hook)
.planning/        — GSD planning files: ROADMAP.md, STATE.md, phase plans and summaries
```

**7. GSD Workflow Notes (per user decision — locked)**
- This project was built with the GSD workflow (`/gsd:plan-phase`)
- Planning files live in `.planning/` — ROADMAP.md, STATE.md, phases/
- To continue development: open a new Claude session → `/gsd:plan-phase` → select next phase
- Each phase: discuss → research → plan → execute → verify
- Do NOT modify `.planning/` files manually during execution — use GSD commands

**8. Environment Variables**
All Firebase config is loaded from `.env` (gitignored). Copy `.env.example` to `.env` and fill in values from Firebase Console → Project Settings → Your apps → Web app → Config.
```
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
```

Section ordering is at Claude's discretion — the above content is required, ordering can be adjusted for readability.
  </action>
  <verify>
Run: `wc -l CLAUDE.md` (should be 80-180 lines)
Run: `grep -c "never" CLAUDE.md` (should be >= 4 matches)
Run: `grep "YOUR-PROJECT.vercel.app" CLAUDE.md` (should find the placeholder comment)
Run: `grep "\.planning" CLAUDE.md` (should find GSD workflow section)
  </verify>
  <done>CLAUDE.md exists with all 8 required sections, never-do list has at least 5 items with reasons, live URL placeholder comment is present, GSD workflow section references .planning/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md and verify environment variable setup</name>
  <files>README.md</files>
  <action>
**Part A — Verify env var state (DEV-04 — already structurally complete per research):**

1. Read `src/firebase/config.ts` — confirm all 6 keys use `import.meta.env.VITE_FIREBASE_*` pattern (already done in Phase 3 — just confirm)
2. Read `.env.example` — confirm it has all 6 `VITE_FIREBASE_*` keys with blank values (already exists — just confirm)
3. Run `grep "^\.env" .gitignore` — confirm `.env` is gitignored
4. Run `git status` — confirm `.env` does NOT appear as a tracked file (it must be ignored, not committed)

If any of these checks fail, fix them. If all pass, proceed to Part B.

**Part B — Create README.md** (portfolio-quality, detailed enough for contributor onboarding):

```markdown
# Tetris

> Neon synthwave Tetris with a real global leaderboard.
>
> **Live:** [https://YOUR-PROJECT.vercel.app](https://YOUR-PROJECT.vercel.app)

A browser-based Tetris implementation featuring the full Tetris Guideline ruleset (SRS rotation with wall kicks, 7-bag randomizer, lock delay, T-spin detection), neon/synthwave aesthetic rendered entirely on HTML5 Canvas, and a Firebase-powered global leaderboard with Google authentication.

## Features

- All 7 standard tetrominoes (I, O, T, S, Z, J, L) with correct SRS rotation + wall kick tables
- 7-bag randomizer for balanced piece distribution
- Hold piece (C key), next 3-piece preview panel
- Ghost piece, hard drop (Space), soft drop (↓)
- Back-to-back Tetris bonus, combo multiplier, T-spin detection
- Guideline scoring: 100/300/500/800 × level for 1/2/3/4 line clears
- Neon glow rendering at 60fps via Canvas 2D (pre-baked OffscreenCanvas textures)
- Google sign-in (Firebase Auth) with session persistence
- Global top-10 leaderboard with real-time updates (Firestore onSnapshot)
- Personal best tracking — scores submitted only if they beat your record
- Keyboard controls + mobile touch controls (swipe + virtual buttons)
- Pause/resume (P key)

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | React 19 + TypeScript 5.7 |
| Build Tool | Vite 6 |
| Rendering | HTML5 Canvas 2D |
| Authentication | Firebase Auth (Google Sign-In) |
| Database | Cloud Firestore |
| Hosting | Vercel |

## Local Setup

### Prerequisites

- Node.js 18+
- A Firebase project with Firestore and Google Auth enabled

### Steps

1. Clone the repo and install dependencies:
   ```bash
   git clone https://github.com/USERNAME/tetris.git
   cd tetris
   npm install
   ```

2. Set up environment variables:
   ```bash
   cp .env.example .env
   # Open .env and fill in your Firebase config values
   ```

3. Start the development server:
   ```bash
   npm run dev
   # Open http://localhost:5173
   ```

### Available Commands

| Command | Description |
|---------|-------------|
| `npm run dev` | Start local dev server (localhost:5173) |
| `npm run build` | TypeScript check + production build |
| `npm test` | Run Vitest unit tests |
| `npm run lint` | ESLint |

## Environment Variables

All Firebase configuration is stored in a `.env` file (gitignored). Copy `.env.example` to `.env` and fill in values from:

**Firebase Console → Project Settings → Your apps → (your web app) → SDK setup and configuration → Config**

```bash
VITE_FIREBASE_API_KEY=          # Web API key
VITE_FIREBASE_AUTH_DOMAIN=      # projectid.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=       # Your Firebase project ID
VITE_FIREBASE_STORAGE_BUCKET=   # projectid.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=  # Sender ID from project settings
VITE_FIREBASE_APP_ID=           # Web app ID
```

Note: `VITE_` prefix is required — Vite only exposes variables with this prefix to client-side code.

## Architecture Notes

### Why Canvas, not DOM cells?

Applying CSS `box-shadow` glow to 200 DOM cells causes a browser paint storm on each frame — measured at ~40fps on mid-range Android devices. The Canvas approach pre-renders each tetromino color as an OffscreenCanvas texture (with `shadowBlur` baked in at startup), then composites them each frame with `drawImage`. Result: consistent 60fps on mobile.

### Why useRef, not useState, for game state?

Calling React's `setState` inside a `requestAnimationFrame` loop causes the component to re-render ~60 times per second. At higher Tetris levels this collapses performance. The game engine and renderer live in `useRef` — they are mutated directly without triggering React re-renders. React `useState` is used only for the score/level/lines/gameOver display values, which update at human-perceptible frequency.

### Why requestAnimationFrame + delta-time?

`setInterval` in JavaScript drifts by tens of milliseconds per tick due to the event loop. At Tetris level 10+, where a piece falls every ~83ms, this drift causes visibly erratic piece movement. `requestAnimationFrame` is the browser's native animation primitive — it fires before each paint and provides a high-resolution timestamp for accurate delta-time calculation.

### Why SRS rotation with wall kick tables?

The Super Rotation System (SRS) is the Tetris Guideline standard used by all modern Tetris implementations. Without wall kick tables, pieces get stuck when rotating near walls or the floor — a jarring experience for players familiar with modern Tetris. This implementation uses the full Guideline wall kick tables for all pieces (with special I-piece offsets).

## Firestore Security

Firestore rules enforce:
- Authenticated writes only (no anonymous writes)
- UID in document path must match the authenticated user's UID
- Score must be a number in range [0, 10,000,000]
- Score can only increase (no score rollbacks)
- No document deletions

## Contributing

1. Fork the repository
2. Create a feature branch: `git checkout -b feature/your-feature`
3. Make your changes following the architecture decisions in CLAUDE.md
4. Run tests: `npm test`
5. Run lint: `npm run lint`
6. Submit a pull request

See CLAUDE.md for architecture decisions, coding conventions, and the never-do list.

## License

MIT
```

Tone: clear and direct, suitable for a portfolio piece. Section ordering is at Claude's discretion. Ensure all markdown renders cleanly.
  </action>
  <verify>
Run: `wc -l README.md` (should be 80+ lines)
Run: `grep "VITE_FIREBASE" README.md` (should find env var section)
Run: `grep "YOUR-PROJECT.vercel.app" README.md` (should find live URL placeholder)
Run: `grep "import.meta.env" src/firebase/config.ts` (confirms DEV-04 already implemented)
Run: `grep "^\.env" .gitignore` (confirms .env is gitignored)
Run: `git status --short` (confirm .env does not appear as an untracked or tracked file)
  </verify>
  <done>README.md exists with all required sections (features, tech stack, local setup, env vars, architecture notes, contributing); src/firebase/config.ts confirmed to use import.meta.env.VITE_* for all 6 keys; .env is gitignored and not tracked in git</done>
</task>

</tasks>

<verification>
1. `ls CLAUDE.md README.md` — both files exist
2. `grep -c "never" CLAUDE.md` — returns >= 4
3. `grep "YOUR-PROJECT.vercel.app" CLAUDE.md` — finds placeholder comment
4. `grep "\.planning" CLAUDE.md` — finds GSD workflow section
5. `grep "VITE_FIREBASE" README.md` — finds env var section
6. `grep "import.meta.env" src/firebase/config.ts` — confirms env var implementation
7. `git status --short` — .env not visible (gitignored)
</verification>

<success_criteria>
- CLAUDE.md exists with architecture decisions, never-do list (5+ items with reasons), GSD workflow notes, live URL placeholder
- README.md exists with features, tech stack, local setup, env var list, architecture notes, contributing guide
- src/firebase/config.ts uses import.meta.env.VITE_* for all 6 Firebase config keys (already done — confirmed)
- .env is gitignored; .env.example is committed with 6 blank keys
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-developer-setup/04-01-SUMMARY.md`
</output>
