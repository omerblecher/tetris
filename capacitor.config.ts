import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.otis.brooke.tetris',
  appName: 'Falling Blocks Classic',
  webDir: 'dist'
};

export default config;
